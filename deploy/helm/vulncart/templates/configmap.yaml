apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "vulncart.fullname" . }}-config
  labels:
    {{- include "vulncart.labels" . | nindent 4 }}
data:
  BRAND: VulnCart
  APP_NAME: vulncart
  # No hardcoded node IPs — Ingress + Service DNS only
  API_PUBLIC_PATH: /api
