# See scenarios.json for runtime SSoT (Guide loads JSON).
# Keep this note file; expand via `pnpm security-guide:expand-scenarios`.
version: 1
note: Canonical runtime file is scenarios.json (36 scenarios covering API1–API10 and Web A01–A10).
